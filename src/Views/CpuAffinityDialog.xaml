<ui:FluentWindow x:Class="ExHyperV.Views.Dialogs.CpuAffinityDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:properties="clr-namespace:ExHyperV.Properties"
                 xmlns:vm="clr-namespace:ExHyperV.ViewModels.Dialogs"
                 mc:Ignorable="d"
                 d:DataContext="{d:DesignInstance vm:CpuAffinityDialogViewModel, IsDesignTimeCreatable=False}"
                 Title="{Binding Title}"
                 Width="800" Height="600"
                 WindowStartupLocation="CenterScreen"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica">

    <Grid Margin="0,30,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{x:Static properties:Resources.SelectHostCpuCoresToBind}" 
                   Margin="20,10,20,10" 
                   TextWrapping="Wrap"
                   Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
        <ItemsControl x:Name="CoresItemsControl" 
                      Grid.Row="1"
                      ItemsSource="{Binding Cores}"
                      Margin="15,0,15,10"
                      PreviewMouseLeftButtonDown="ItemsControl_PreviewMouseLeftButtonDown"
                      PreviewMouseMove="ItemsControl_PreviewMouseMove"
                      PreviewMouseLeftButtonUp="ItemsControl_PreviewMouseLeftButtonUp">
            <ItemsControl.Resources>
                <Style x:Key="SelectableCoreBoxStyle" TargetType="CheckBox">
                    <Setter Property="Margin" Value="3"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="CheckBox">
                                <Border x:Name="RootBorder" 
                                        CornerRadius="4" 
                                        BorderThickness="1" 
                                        BorderBrush="#1A888888" 
                                        Background="#08FFFFFF">
                                    <Grid>
                                        <TextBlock Text="{Binding CoreId}"
                                                   FontSize="16" FontWeight="SemiBold" 
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                   Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>

                                        <TextBlock FontSize="12" Margin="5"
                                                   HorizontalAlignment="Left" VerticalAlignment="Top" 
                                                   FontWeight="Bold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding CoreType}" Value="Performance">
                                                            <Setter Property="Text" Value="⚡"/>
                                                            <Setter Property="Foreground" Value="#FFFAA146"/>
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding CoreType}" Value="Efficient">
                                                            <Setter Property="Text" Value="🍃"/>
                                                            <Setter Property="Foreground" Value="#FF107C10"/>
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="RootBorder" Property="BorderBrush" Value="#FF54C7FC"/>
                                        <Setter TargetName="RootBorder" Property="Background" Value="#33117DBB"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="RootBorder" Property="Background" Value="#1AFFFFFF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ItemsControl.Resources>

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="{Binding Columns}" Rows="{Binding Rows}"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" 
                              Style="{StaticResource SelectableCoreBoxStyle}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="{ui:ThemeResource ControlStrokeColorDefaultBrush}" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding StatusEmoji}" FontSize="16" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding StatusText}" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <ui:Button Content="{x:Static properties:Resources.sure}" Appearance="Primary" Margin="0,0,10,0" Click="ConfirmButton_Click"/>
                    <ui:Button Content="{x:Static properties:Resources.Button_Cancel}" Click="CancelButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>