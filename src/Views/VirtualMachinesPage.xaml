<Page
    x:Class="ExHyperV.Views.Pages.VirtualMachinesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:properties="clr-namespace:ExHyperV.Properties"
    xmlns:converters="clr-namespace:ExHyperV.Converters"
    xmlns:tools="clr-namespace:ExHyperV.Tools"
    xmlns:components="clr-namespace:ExHyperV.Views.Components"
    Title="VirtualMachinesPage" mc:Ignorable="d" d:DesignHeight="750" d:DesignWidth="1100"
    tools:ScrollHelper.LockParentScroll="True"
    x:Name="RootPage">

    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVis" />
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <converters:VmNameToIconConverter x:Key="VmNameToIconConverter" />
        <converters:OsTypeToImageConverter x:Key="OsTypeToImageConverter" />

        <DataTemplate x:Key="VmListItemTemplate">
            <Grid Margin="2,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Width="4" Height="4" CornerRadius="2" VerticalAlignment="Center" Margin="0,0,8,0">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Background" Value="{ui:ThemeResource TextFillColorTertiaryBrush}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                    <Setter Property="Background" Value="{ui:ThemeResource SystemFillColorSuccessBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                </Border>
                <ui:FontIcon Grid.Column="1" FontFamily="{DynamicResource SegoeFluentIcons}" 
                             Glyph="{Binding Name, Converter={StaticResource VmNameToIconConverter}}" 
                             FontSize="16" Margin="0,0,8,0"
                             Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                <TextBlock Grid.Column="2" Text="{Binding Name}" VerticalAlignment="Center" FontSize="13" 
                           TextTrimming="CharacterEllipsis"
                           Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="VmDetailTemplate">
            <Grid x:Name="DetailRootGrid">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Margin="24,12,24,0" 
                        Background="{ui:ThemeResource ControlFillColorSecondaryBrush}" 
                        BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" 
                        CornerRadius="8" Padding="24" MinHeight="160">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Image Grid.Column="0" Source="{Binding OsType, Converter={StaticResource OsTypeToImageConverter}}" 
                               RenderOptions.BitmapScalingMode="HighQuality" 
                               Width="100" Height="100" VerticalAlignment="Center"/>

                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Name}" FontSize="28" 
                                       Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}" Margin="0,0,0,8"/>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <ui:SymbolIcon Symbol="Pulse24" FontSize="16" Margin="0,1,6,0" 
                                               Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{Binding State}" FontSize="14" 
                                           Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" Margin="0,0,12,0"/>

                                <ui:SymbolIcon Symbol="Timer24" FontSize="16" Margin="0,1,6,0" 
                                               Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{Binding Uptime}" FontSize="14" 
                                           Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <Border Background="{ui:ThemeResource ControlFillColorTertiaryBrush}" CornerRadius="4" Padding="10,6" Margin="0,0,8,0">
                                    <StackPanel Orientation="Horizontal">
                                        <ui:FontIcon Glyph="&#xE81E;" FontSize="14" FontFamily="{DynamicResource SegoeFluentIcons}" Margin="0,1,8,0" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                                        <TextBlock Text="{Binding ConfigSummary}" FontSize="13" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                                    </StackPanel>
                                </Border>

                                <Border Background="{ui:ThemeResource ControlFillColorTertiaryBrush}" CornerRadius="4" Padding="10,6" Margin="0,0,8,0">
                                    <StackPanel Orientation="Horizontal">
                                        <ui:FontIcon Glyph="&#xE975;" FontSize="14" FontFamily="{DynamicResource SegoeFluentIcons}" Margin="0,1,8,0" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                                        <TextBlock Text="{Binding Generation, StringFormat='{}{0}代'}" FontSize="13" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                                    </StackPanel>
                                </Border>

                                <Border Background="{ui:ThemeResource ControlFillColorTertiaryBrush}" CornerRadius="4" Padding="10,6">
                                    <StackPanel Orientation="Horizontal">
                                        <ui:FontIcon Glyph="&#xE946;" FontSize="14" FontFamily="{DynamicResource SegoeFluentIcons}" Margin="0,1,8,0" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                                        <TextBlock Text="{Binding Version, StringFormat='{}配置版本 {0}'}" FontSize="13" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal">
                                <ui:DropDownButton Appearance="Secondary" Padding="10,6" CornerRadius="4">
                                    <ui:DropDownButton.Content>
                                        <StackPanel Orientation="Horizontal">
                                            <ui:FontIcon Glyph="&#xE74C;" FontSize="14" FontFamily="{DynamicResource SegoeFluentIcons}" Margin="0,1,8,0" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                                            <TextBlock Text="{Binding OsType}" FontSize="13" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                        </StackPanel>
                                    </ui:DropDownButton.Content>
                                    <ui:DropDownButton.Flyout>
                                        <ContextMenu ItemsSource="{Binding DataContext.AvailableOsTypes, Source={x:Reference RootPage}}">
                                            <ContextMenu.ItemContainerStyle>
                                                <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                                                    <Setter Property="Header" Value="{Binding}" />
                                                    <Setter Property="Command" Value="{Binding DataContext.ChangeOsTypeCommand, Source={x:Reference RootPage}}" />
                                                    <Setter Property="CommandParameter" Value="{Binding}" />
                                                    <Setter Property="Icon" Value="{x:Null}"/>
                                                </Style>
                                            </ContextMenu.ItemContainerStyle>
                                        </ContextMenu>
                                    </ui:DropDownButton.Flyout>
                                </ui:DropDownButton>

                                <!-- ======== 修改后的 GPU 标签代码 ======== -->
                                <Border Margin="8,0,0,0" VerticalAlignment="Center" 
        Background="{ui:ThemeResource ControlFillColorTertiaryBrush}" 
        CornerRadius="4" Padding="10,6">

                                    <StackPanel Orientation="Horizontal">
                                        <ui:FontIcon Glyph="&#xF211;" FontSize="14" FontFamily="{DynamicResource SegoeFluentIcons}" Margin="0,1,8,0" 
                     Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>

                                        <TextBlock FontSize="13">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <!-- 默认情况：显示显卡名称，颜色为主要文本色 -->
                                                    <Setter Property="Text" Value="{Binding GpuName}"/>
                                                    <Setter Property="Foreground" Value="{ui:ThemeResource TextFillColorSecondaryBrush}"/>

                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HasGpu}" Value="False">
                                                            <Setter Property="Text" Value="无"/>
                                                            <Setter Property="Foreground" Value="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                                <!-- ===================================== -->
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Top">
                            <Border CornerRadius="8" ClipToBounds="True">
                                <Grid>
                                    <Grid Visibility="Hidden" IsHitTestVisible="False">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <ui:Button Grid.Column="0" Content="关机" Icon="Stop24" Appearance="Danger" Padding="24,6" Height="36" BorderThickness="0" Margin="0"/>
                                        <ui:DropDownButton Grid.Column="1" Icon="ChevronDown24" Appearance="Danger" Padding="8,6" Height="36" BorderThickness="0" Margin="0"/>
                                    </Grid>

                                    <ui:Button Content="启动" Icon="Play24" Appearance="Primary" 
                                               Padding="0,6" Height="36" CornerRadius="8" BorderThickness="0"
                                               HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"
                                               Command="{Binding ControlCommand}" 
                                               CommandParameter="Start">
                                        <ui:Button.Style>
                                            <Style TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsRunning}" Value="False">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ui:Button.Style>
                                    </ui:Button>

                                    <Grid>
                                        <Grid.Style>
                                            <Style TargetType="Grid">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Grid.Style>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <ui:Button Grid.Column="0" Content="关机" Icon="Stop24" Appearance="Danger" 
                                                   Padding="24,6" Height="36" CornerRadius="8,0,0,8" BorderThickness="0" Margin="0"
                                                   Command="{Binding ControlCommand}" 
                                                   CommandParameter="Stop"/>
                                        <ui:DropDownButton Grid.Column="1" Icon="ChevronDown24" Appearance="Danger" 
                                                           Padding="8,6" Height="36" CornerRadius="0,8,8,0" BorderThickness="0" Margin="0">
                                            <ui:DropDownButton.Flyout>
                                                <ContextMenu>
                                                    <MenuItem Header="重启" Icon="{ui:SymbolIcon ArrowCounterclockwise24}"
                                                              Command="{Binding PlacementTarget.DataContext.ControlCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" 
                                                              CommandParameter="Restart"/>
                                                    <MenuItem Header="暂停" Icon="{ui:SymbolIcon Pause24}"
                                                              Command="{Binding PlacementTarget.DataContext.ControlCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" 
                                                              CommandParameter="Suspend"/>
                                                    <Separator/>
                                                    <MenuItem Header="保存状态" Icon="{ui:SymbolIcon Save24}"
                                                              Command="{Binding PlacementTarget.DataContext.ControlCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" 
                                                              CommandParameter="Save"/>
                                                    <MenuItem Header="断电 (强制关闭)" Icon="{ui:SymbolIcon Power24}" Foreground="#E81123"
                                                              Command="{Binding PlacementTarget.DataContext.ControlCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" 
                                                              CommandParameter="TurnOff"/>
                                                </ContextMenu>
                                            </ui:DropDownButton.Flyout>
                                        </ui:DropDownButton>
                                    </Grid>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>

                <Rectangle Grid.Row="1" Height="1" Fill="{ui:ThemeResource CardStrokeColorDefaultBrush}" Margin="24,12,24,0" Opacity="0.5"/>

                <ContentControl Grid.Row="2" Content="{Binding DataContext, RelativeSource={RelativeSource AncestorType=Page}}">
                    <ContentControl.Style>
                        <Style TargetType="ContentControl">
                            <Setter Property="ContentTemplate">
                                <Setter.Value>
                                    <DataTemplate>
                                        <components:VmDashboardView/>
                                    </DataTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding DataContext.CurrentViewType, RelativeSource={RelativeSource AncestorType=Page}}" Value="CpuSettings">
                                    <Setter Property="ContentTemplate">
                                        <Setter.Value>
                                            <DataTemplate>
                                                <components:VmCpuSettingsView/>
                                            </DataTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding DataContext.CurrentViewType, RelativeSource={RelativeSource AncestorType=Page}}" Value="CpuAffinity">
                                    <Setter Property="ContentTemplate">
                                        <Setter.Value>
                                            <DataTemplate>
                                                <components:VmCpuAffinityView/>
                                            </DataTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding DataContext.CurrentViewType, RelativeSource={RelativeSource AncestorType=Page}}" Value="MemorySettings">
                                    <Setter Property="ContentTemplate">
                                        <Setter.Value>
                                            <DataTemplate>
                                                <components:VmRAMSettingsView/>
                                            </DataTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ContentControl.Style>
                </ContentControl>

                <Grid Grid.Row="0" Grid.RowSpan="3" Panel.ZIndex="100"
                      Background="{ui:ThemeResource ApplicationBackgroundBrush}" 
                      Visibility="{Binding DataContext.IsLoading, RelativeSource={RelativeSource AncestorType=Page}, Converter={StaticResource BoolToVis}}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <ui:ProgressRing IsIndeterminate="True" Width="40" Height="40" Margin="0,0,0,16"/>
                        <TextBlock Text="{x:Static properties:Resources.DataLoading}" 
                                   Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" MinWidth="160" MaxWidth="300"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{ui:ThemeResource ControlFillColorDefaultBrush}" 
                BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="8">
                    <ui:TextBox PlaceholderText="搜索虚拟机" ClearButtonEnabled="True" Padding="10,0,32,0"
                                Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <ui:SymbolIcon Symbol="Search24" HorizontalAlignment="Right" Margin="0,0,10,0" 
                                   Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" IsHitTestVisible="False"/>
                </Grid>
                <ui:ListView Grid.Row="1" ItemsSource="{Binding VmList}" SelectedItem="{Binding SelectedVm, Mode=TwoWay}" 
                             ItemTemplate="{StaticResource VmListItemTemplate}" Margin="4,0,4,4"/>
            </Grid>
        </Border>

        <GridSplitter Grid.Column="1" Width="12" HorizontalAlignment="Stretch" Background="Transparent"/>

        <Border Grid.Column="2" Background="{ui:ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" ClipToBounds="True">
            <ContentControl Content="{Binding SelectedVm}" ContentTemplate="{StaticResource VmDetailTemplate}"/>
        </Border>
    </Grid>
</Page>