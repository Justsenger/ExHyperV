<Page
    x:Class="ExHyperV.Views.Pages.GpuPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:markup="clr-namespace:ExHyperV.Markup"
    Title="GPUPage"
    d:DesignHeight="700"
    d:DesignWidth="1000"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ProgressBar Grid.Row="0" x:Name="Progressbar" IsIndeterminate="True" />

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ui:Card VerticalAlignment="Top" Padding="12" Margin="10,10,10,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
                    <ui:FontIcon FontSize="24" FontFamily="{DynamicResource SegoeFluentIcons}" Glyph="&#xF211;" />
                    <TextBlock Text="{markup:Localized installedcard}" Margin="10,0,0,0" FontSize="16" />
                </StackPanel>
            </ui:Card>
            <Grid x:Name="Main" Grid.Row="1" />

        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <ui:Card VerticalAlignment="Top" Padding="10" Grid.Column="0" Margin="10,10,10,0" Grid.ColumnSpan="2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <ui:FontIcon FontSize="24" FontFamily="{DynamicResource SegoeFluentIcons}" Glyph="&#xE7C4;" />
                    <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="{markup:Localized vmlist}"
                               Margin="10,0,0,0" FontSize="16" />
                    <ui:Button Grid.Column="2" Content="{markup:Localized refresh}" Margin="0,0,30,0"
                               Click="VmRefresh" />

                </Grid>
            </ui:Card>

            <Grid x:Name="Vms" Grid.Row="0" Grid.ColumnSpan="2" Grid.Column="0" Margin="0,63,0,0" />

        </Grid>


    </Grid>


</Page>