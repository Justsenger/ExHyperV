<Page
    x:Class="ExHyperV.Views.Pages.DdaPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:markup="clr-namespace:ExHyperV.Markup"
    Title="MainPage"
    d:DesignHeight="700"
    d:DesignWidth="1000"
    mc:Ignorable="d">

    <Grid Margin="10" VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition Height="2" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ProgressBar Grid.Row="0" x:Name="ProgressRing" IsIndeterminate="True" />

        <ui:InfoBar Grid.Row="1" Margin="0,5,0,0"
                    Title="{markup:Localized attention}"
                    Message="{markup:Localized attentionwords}"
                    IsOpen="True"
                    Severity="Warning" />
        <ui:InfoBar Grid.Row="2" Margin="0,5,0,0" x:Name="Isserver"
                    Title="{markup:Localized error}"
                    Message="{markup:Localized servererror}"
                    IsOpen="True"
                    IsClosable="False"
                    Severity="Error" />
        <Grid Grid.Row="3" Margin="0,5,0,0">
            <ui:Card BorderThickness="1" Padding="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <!-- System info text occupies left space -->
                        <ColumnDefinition Width="Auto" />
                        <!-- Refresh button auto-width -->
                    </Grid.ColumnDefinitions>

                    <!-- Hardware info content -->
                    <Grid Grid.Column="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <ui:FontIcon FontSize="24" FontFamily="{DynamicResource SegoeFluentIcons}" Glyph="&#xE964;" />
                        <TextBlock Grid.Column="1" Text="{markup:Localized Assignabledevices}"
                                   FontSize="16" VerticalAlignment="Center" Margin="10,0,0,0" />
                        <ui:Button Grid.Column="2" Content="{markup:Localized refresh}" Margin="0,0,10,0"
                                   Click="DdArefresh" />
                    </Grid>

                </Grid>
            </ui:Card>

        </Grid>

        <Grid x:Name="ParentPanel" Grid.Row="4" />

    </Grid>


</Page>