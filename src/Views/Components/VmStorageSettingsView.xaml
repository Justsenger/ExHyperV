<UserControl x:Class="ExHyperV.Views.Components.VmStorageSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="1000">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- 标题 -->
            <RowDefinition Height="Auto"/>
            <!-- 加载条 -->
            <RowDefinition Height="*"/>
            <!-- 内容 -->
        </Grid.RowDefinitions>

        <!-- 1. 顶部标题栏 -->
        <Grid Grid.Row="0" Margin="24,16,24,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="存储设置" FontSize="20" FontWeight="Regular" VerticalAlignment="Center" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
            <ui:Button Grid.Column="1" Appearance="Transparent" Padding="8" Command="{Binding GoBackToDashboardCommand}">
                <ui:SymbolIcon Symbol="ArrowLeft24" FontSize="18"/>
            </ui:Button>
        </Grid>

        <!-- 2. 模拟加载进度条 -->
        <ProgressBar Grid.Row="1" IsIndeterminate="{Binding IsLoadingSettings}" VerticalAlignment="Top" Height="2" Margin="0,-2,0,0"
                     Visibility="Visible"/>

        <!-- 3. 主滚动区域 -->
        <ScrollViewer Grid.Row="2" Padding="24,12,24,24" VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <!-- 磁盘列表区域 -->
                <TextBlock Text="已挂载的虚拟磁盘" FontSize="14" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" Margin="0,0,0,12"/>

                <!-- 直接绑定你已有的 Disks 集合 -->
                <ItemsControl ItemsSource="{Binding SelectedVm.Disks}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ui:Card Margin="0,0,0,12" Padding="16">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <!-- 图标 -->
                                        <ColumnDefinition Width="*"/>
                                        <!-- 信息 -->
                                        <ColumnDefinition Width="Auto"/>
                                        <!-- 操作 -->
                                    </Grid.ColumnDefinitions>

                                    <ui:FontIcon Glyph="&#xEDA2;" FontSize="28" Margin="0,0,16,0"/>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14"/>
                                        <Grid Margin="0,4,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBox Text="{Binding Path}" IsReadOnly="True" FontSize="12" Opacity="0.7" Margin="0,0,8,0"/>
                                            <ui:Button Grid.Column="1" Content="浏览..." Padding="12,4"/>
                                        </Grid>
                                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                            <Border Background="#10888888" CornerRadius="4" Padding="6,2">
                                                <TextBlock Text="{Binding DiskType}" FontSize="11" Opacity="0.6"/>
                                            </Border>
                                            <TextBlock Text="{Binding UsageText}" FontSize="11" Margin="12,0,0,0" VerticalAlignment="Center" Opacity="0.6"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <ui:Button Grid.Column="2" Appearance="Transparent" Foreground="{ui:ThemeResource SystemFillColorCriticalBrush}" 
                                               VerticalAlignment="Top" Icon="{ui:SymbolIcon Symbol=Delete24}"/>
                                </Grid>
                            </ui:Card>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- 模拟添加磁盘按钮 -->
                <ui:Button HorizontalAlignment="Stretch" Margin="0,4,0,32" Appearance="Secondary" Icon="{ui:SymbolIcon Symbol=Add24}" Content="添加新的虚拟磁盘驱动器"/>

                <!-- 模拟全局管理设置 -->
                <TextBlock Text="管理设置" FontSize="14" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" Margin="0,0,0,12"/>
                <ui:Card Padding="16">
                    <StackPanel>
                        <TextBlock Text="智能分页文件位置" FontSize="14" FontWeight="SemiBold"/>
                        <TextBlock Text="当内存不足以启动虚拟机时，用于存放临时分页数据的路径。" FontSize="12" Opacity="0.6" Margin="0,2,0,12"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <!-- 这里的 Text 先随便写一个路径，看效果用 -->
                            <TextBox Text="C:\ProgramData\Microsoft\Windows\Hyper-V\Snapshots" IsReadOnly="True"/>
                            <ui:Button Grid.Column="1" Margin="8,0,0,0" Content="更改位置..."/>
                        </Grid>
                    </StackPanel>
                </ui:Card>

                <!-- 底部操作栏 -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,32,0,0">
                    <ui:Button Content="取消更改" Appearance="Transparent" Margin="0,0,12,0"/>
                    <ui:Button Content="保存所有存储配置" Appearance="Primary" Padding="32,8"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>