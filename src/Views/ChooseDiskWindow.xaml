<ui:FluentWindow x:Class="ExHyperV.Views.ChooseDiskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:converters="clr-namespace:ExHyperV.Converters"
        xmlns:properties="clr-namespace:ExHyperV.Properties"
        x:Name="AddDiskDialog"
        Title="添加存储设备" Height="580" Width="450"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">

    <ui:FluentWindow.Resources>
        <converters:EqualityConverter x:Key="EqualityConverter" />
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Title="添加存储设备" Grid.Row="0">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Assets/Icon.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <ScrollViewer Grid.Row="1" Margin="15,10,15,0" VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <ui:CardExpander Padding="15,8" Margin="0,0,0,8" IsExpanded="False" HorizontalContentAlignment="Stretch">
                    <ui:CardExpander.Header>
                        <Grid HorizontalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="插槽配置" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <CheckBox Grid.Column="1" Content="自动分配" IsChecked="{Binding AutoAssign}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        </Grid>
                    </ui:CardExpander.Header>

                    <Grid IsEnabled="{Binding AutoAssign, Converter={StaticResource InverseBooleanConverter}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="接口" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="12" Opacity="0.6"/>
                        <ComboBox Grid.Column="1" ItemsSource="{Binding AvailableControllerTypes}" SelectedItem="{Binding SelectedControllerType}"/>

                        <TextBlock Grid.Column="3" Text="编号" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="12" Opacity="0.6"/>
                        <ComboBox Grid.Column="4" ItemsSource="{Binding AvailableControllerNumbers}" SelectedItem="{Binding SelectedControllerNumber}"/>

                        <TextBlock Grid.Column="6" Text="位置" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="12" Opacity="0.6"/>
                        <ComboBox Grid.Column="7" 
                          ItemsSource="{Binding AvailableLocations}" 
                          SelectedItem="{Binding SelectedLocation, Mode=TwoWay}"/>
                    </Grid>
                </ui:CardExpander>

                <ui:Card Padding="15" Margin="0,0,0,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="类型" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <StackPanel Orientation="Horizontal">
                                <RadioButton Content="硬盘" Margin="0,0,-12,0" IsChecked="{Binding DeviceType, Converter={StaticResource EqualityConverter}, ConverterParameter='HardDisk'}"/>
                                <RadioButton Content="光驱" IsChecked="{Binding DeviceType, Converter={StaticResource EqualityConverter}, ConverterParameter='DvdDrive'}"/>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="来源" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <StackPanel Orientation="Horizontal">
                                <RadioButton Content="虚拟文件" Margin="0,0,-12,0" IsChecked="{Binding IsPhysicalSource, Converter={StaticResource InverseBooleanConverter}}"/>
                                <RadioButton Content="物理设备" IsChecked="{Binding IsPhysicalSource}"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </ui:Card>

                <ui:Card Padding="15" Margin="0,0,0,8">
                    <Grid MinHeight="100">
                        <StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsPhysicalSource}" Value="False">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>

                            <Grid Margin="0,0,0,10">
                                <TextBlock Text="媒介选择" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                <CheckBox Content="新建" IsChecked="{Binding IsNewDisk}" HorizontalAlignment="Right">
                                    <CheckBox.Style>
                                        <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
                                            <Setter Property="Visibility" Value="Hidden"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding DeviceType}" Value="HardDisk">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </CheckBox.Style>
                                </CheckBox>
                            </Grid>

                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ui:TextBox Text="{Binding FilePath}" PlaceholderText="{Binding FilePathPlaceholder}" VerticalAlignment="Center"/>
                                <ui:Button Grid.Column="1" Content="浏览" Margin="10,0,0,0" Width="60" Command="{Binding BrowseFileCommand}"/>
                            </Grid>

                            <Grid Margin="0,0,0,10">
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsNewDisk}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedVhdType}" Value="Differencing">
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                    <Setter Property="Opacity" Value="0.6"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <TextBlock Text="容量 (GB)" FontSize="12" Opacity="0.7" Margin="0,0,0,5"/>
                                    <ComboBox ItemsSource="{Binding NewDiskSizePresets}" Text="{Binding NewDiskSize, Mode=TwoWay}" IsEditable="True" HorizontalAlignment="Stretch"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="磁盘类型" FontSize="12" Opacity="0.7" Margin="0,0,0,5"/>
                                    <ComboBox SelectedValuePath="Tag" SelectedValue="{Binding SelectedVhdType}" HorizontalAlignment="Stretch">
                                        <ComboBoxItem Content="动态扩展" Tag="Dynamic"/>
                                        <ComboBoxItem Content="固定大小" Tag="Fixed"/>
                                        <ComboBoxItem Content="差分磁盘" Tag="Differencing"/>
                                    </ComboBox>
                                </StackPanel>
                            </Grid>

                            <Grid Margin="0,0,0,5">
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding IsNewDisk}" Value="True"/>
                                                    <Condition Binding="{Binding SelectedVhdType}" Value="Differencing"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ui:TextBox Text="{Binding ParentPath}" PlaceholderText="请指定父磁盘路径..." VerticalAlignment="Center"/>
                                <ui:Button Grid.Column="1" Content="浏览" Margin="10,0,0,0" Width="60" Command="{Binding BrowseParentFileCommand}"/>
                            </Grid>

                            <Grid Margin="0,0,0,10">
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsNewDisk}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedVhdType}" Value="Differencing">
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                    <Setter Property="Opacity" Value="0.6"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <TextBlock Text="扇区格式" FontSize="12" Opacity="0.7" Margin="0,0,0,5"/>
                                    <ComboBox SelectedValue="{Binding SectorFormat}" 
                                          SelectedValuePath="Tag" 
                                          HorizontalAlignment="Stretch">
                                        <ComboBoxItem Content="默认" Tag="Default"/>
                                        <ComboBoxItem Content="512n" Tag="512n"/>
                                        <ComboBoxItem Content="512e" Tag="512e"/>
                                        <ComboBoxItem Content="4Kn" Tag="4kn"/>
                                    </ComboBox>
                                </StackPanel>

                                <StackPanel Grid.Column="1">
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedVhdType}" Value="Differencing">
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                    <Setter Property="Opacity" Value="0.6"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <TextBlock Text="块大小" FontSize="12" Opacity="0.7" Margin="0,0,0,5"/>
                                    <ComboBox SelectedValue="{Binding BlockSize, Mode=TwoWay}" 
          SelectedValuePath="Tag" 
          HorizontalAlignment="Stretch">
                                        <ComboBoxItem Content="默认" Tag="Default"/>
                                        <ComboBoxItem Content="1MB" Tag="1048576"/>
                                        <ComboBoxItem Content="2MB" Tag="2097152"/>
                                        <ComboBoxItem Content="4MB" Tag="4194304"/>
                                        <ComboBoxItem Content="8MB" Tag="8388608"/>
                                        <ComboBoxItem Content="16MB" Tag="16777216"/>
                                        <ComboBoxItem Content="32MB" Tag="33554432"/>
                                        <ComboBoxItem Content="64MB" Tag="67108864"/>
                                        <ComboBoxItem Content="128MB" Tag="134217728"/>
                                        <ComboBoxItem Content="256MB" Tag="268435456"/>
                                    </ComboBox>
                                </StackPanel>
                            </Grid>

                        </StackPanel>

                        <Grid>
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsPhysicalSource}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>
                            <TextBlock Text="无可用物理设备" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HostDisks.Count}" Value="0">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <Border BorderThickness="1" BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}" CornerRadius="4" MaxHeight="280">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HostDisks.Count}" Value="0">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <ui:ListView ItemsSource="{Binding HostDisks}" SelectedItem="{Binding SelectedPhysicalDisk}" ScrollViewer.VerticalScrollBarVisibility="Auto">
                                    <ui:ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="10,5">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <ui:FontIcon Glyph="&#xEDA2;" FontFamily="{StaticResource SegoeFluentIcons}" FontSize="24" VerticalAlignment="Center"/>
                                                <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                                    <TextBlock Text="{Binding FriendlyName}" FontWeight="Medium"/>
                                                    <StackPanel Orientation="Horizontal" Opacity="0.7">
                                                        <TextBlock Text="磁盘 "/>
                                                        <TextBlock Text="{Binding Number}"/>
                                                        <TextBlock Text=" | "/>
                                                        <TextBlock Text="{Binding SizeGB, StringFormat='{}{0:F2} GB'}"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Grid>
                                        </DataTemplate>
                                    </ui:ListView.ItemTemplate>
                                </ui:ListView>
                            </Border>
                        </Grid>
                    </Grid>
                </ui:Card>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="2" Margin="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="{x:Static properties:Resources.sure}" Appearance="Primary" Width="80" Command="{Binding ConfirmCommand}" CommandParameter="{Binding ElementName=AddDiskDialog}"/>
                <ui:Button Content="{x:Static properties:Resources.cancel}" Width="80" Margin="10,0,0,0" Click="CancelButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>