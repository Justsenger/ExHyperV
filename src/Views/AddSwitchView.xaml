<UserControl x:Class="ExHyperV.Views.AddSwitchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:viewModels="clr-namespace:ExHyperV.ViewModels"
             xmlns:converters="clr-namespace:ExHyperV.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:AddSwitchViewModel, IsDesignTimeCreatable=False}"
             MinWidth="400">

	<UserControl.Resources>
		<ResourceDictionary>
			<converters:EqualityConverter x:Key="Equality" />
			<converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
		</ResourceDictionary>
	</UserControl.Resources>

	<Grid>
		<StackPanel>
			<Grid Margin="0,0,0,10">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="120"/>
					<ColumnDefinition Width="*"/>
				</Grid.ColumnDefinitions>
				<TextBlock Grid.Column="0" Text="交换机名称" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
				<ui:TextBox Grid.Column="1" Text="{Binding SwitchName, UpdateSourceTrigger=PropertyChanged}"/>
			</Grid>

			<Grid Margin="0,0,0,10">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="120"/>
					<ColumnDefinition Width="*"/>
				</Grid.ColumnDefinitions>
				<TextBlock Grid.Column="0" Text="交换机类型" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
				<StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
					<RadioButton Content="外部" Margin="0,0,15,0" IsChecked="{Binding SelectedSwitchType, Converter={StaticResource Equality}, ConverterParameter=External}" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
					<RadioButton Content="NAT" Margin="0,0,15,0" IsChecked="{Binding SelectedSwitchType, Converter={StaticResource Equality}, ConverterParameter=NAT}" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
					<RadioButton Content="无上游" IsChecked="{Binding SelectedSwitchType, Converter={StaticResource Equality}, ConverterParameter=Internal}" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
				</StackPanel>
			</Grid>

			<Grid Margin="0,0,0,10">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="120"/>
					<ColumnDefinition Width="*"/>
				</Grid.ColumnDefinitions>
				<TextBlock Grid.Column="0" Text="物理网卡" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
				<ComboBox Grid.Column="1"
                          ItemsSource="{Binding AvailableNetworkAdapters}"
                          SelectedItem="{Binding SelectedNetworkAdapter}"
                          IsEnabled="{Binding IsNetworkAdapterSelectionEnabled}"/>
			</Grid>

			<TextBlock Text="{Binding ErrorMessage}"
                       Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                       TextWrapping="Wrap"
                       MinHeight="20"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
		</StackPanel>
	</Grid>
</UserControl>