<Page x:Class="ExHyperV.Views.Pages.CpuPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:viewmodels="clr-namespace:ExHyperV.ViewModels"
      xmlns:converters="clr-namespace:ExHyperV.Converters"
      xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.Wpf"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance Type=viewmodels:CpuPageViewModel, IsDesignTimeCreatable=True}">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Dark"/>
                <ui:ControlsDictionary/>
            </ResourceDictionary.MergedDictionaries>

            <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>

            <DataTemplate DataType="{x:Type viewmodels:HostCpuViewModel}">
                <StackPanel Margin="5">
                    <TextBlock Text="{Binding CpuName}" FontSize="16" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalUsage, StringFormat={}{0:F1}%}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,2,0,5"/>
                    <lvc:CartesianChart
                        Series="{Binding TotalUsageSeries}"
                        Height="40" XAxes="{x:Null}" YAxes="{x:Null}" AnimationsSpeed="0:0:0.100" Tooltip="{x:Null}" Legend="{x:Null}"/>
                </StackPanel>
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewmodels:VmCpuViewModel}">
                <StackPanel Margin="5">
                    <TextBlock Text="{Binding VmName}" FontSize="16" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding AverageUsage, StringFormat=平均负载: {0:F1}%}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,2,0,5"/>
                    <lvc:CartesianChart
                        Series="{Binding Series}"
                        Height="40" XAxes="{x:Null}" YAxes="{x:Null}" AnimationsSpeed="0:0:0.100" Tooltip="{x:Null}" Legend="{x:Null}"/>
                </StackPanel>
            </DataTemplate>
        </ResourceDictionary>
    </Page.Resources>

    <Grid Margin="20">
        <Grid Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ListBox Grid.Column="0" ItemsSource="{Binding PerformanceItems}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.Resources>
                    <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                        <Setter Property="Padding" Value="5"/>
                        <Setter Property="Margin" Value="0,0,0,5"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Bd" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="true">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SystemAccentColorLight2Brush}"/>
                                            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="true">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.Resources>
            </ListBox>

            <Border Grid.Column="2" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4">
                <ContentControl Content="{Binding SelectedItem}">
                    <ContentControl.Resources>
                        <DataTemplate DataType="{x:Type viewmodels:HostCpuViewModel}">
                            <Grid Margin="15">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="逻辑处理器" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>

                                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                    <ItemsControl ItemsSource="{Binding Cores}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <UniformGrid Columns="4" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type viewmodels:CoreViewModel}">
                                                <Border BorderBrush="{DynamicResource ControlStrokeColorSecondaryBrush}" BorderThickness="0.5" Margin="5">
                                                    <!-- ▼▼▼ 【最终修复】这里只使用绑定，不创建任何对象 ▼▼▼ -->
                                                    <lvc:CartesianChart
                                                        Series="{Binding Series}"
                                                        XAxes="{Binding XAxes}"
                                                        YAxes="{Binding YAxes}"
                                                        Background="Transparent"
                                                        DrawMarginFrame="{x:Null}"
                                                        AnimationsSpeed="0:0:0.100" 
                                                        Tooltip="{x:Null}" 
                                                        Legend="{x:Null}" />
                                                    <!-- ▲▲▲ 【最终修复】结束 ▲▲▲ -->
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Grid>
                        </DataTemplate>

                        <DataTemplate DataType="{x:Type viewmodels:VmCpuViewModel}">
                            <TextBlock Text="{Binding VmName, StringFormat='这里是虚拟机 {0} 的详细图表 (待实现)'}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                        </DataTemplate>
                    </ContentControl.Resources>
                </ContentControl>
            </Border>
        </Grid>
        <ui:ProgressRing IsIndeterminate="True" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
</Page>